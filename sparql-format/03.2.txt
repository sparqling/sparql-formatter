#!/usr/bin/env spang2
# @option --fmt

PREFIX dc: <http://purl.org/dc/elements/1.1/>
PREFIX ns: <http://example.org/ns#>

SELECT ?title ?price
WHERE {
  ?x ns:price ?price .
  ?x dc:title ?title .
  FILTER (?price < 30.5)
}
